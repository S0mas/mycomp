# Virtual environment
.venv/

# Python cache
__pycache__/
*.pyc
*.pyo
*.pyd
.Python

# Runtime secrets
.env
.claude/settings.local.json

# Runtime state — generated by `./mycomp init`
# Skills, persons, teams, and policies are committed defaults; only state.yaml is runtime.
company/state.yaml

# Runtime project data — generated per project execution
projects/
workspace/
reqs*.md
tmp/

# Temporary agent plan files written during planning
cto_plan.json
hr_team.json
hr_team_review.json
dedup_plan.json

# cloudflared tunnel binary (downloaded, not committed)
cloudflared
.mcp_tunnel_url
.mcp_server.pid
.mcp_tunnel.pid
